name: NuGet Package CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: NuGet Pack Raspbian
      run: | 
        cd BuildNuGetPackages/BuildNuGetRaspbian/
        dotnet pack --output packages
        cd packages
        ls
    - name: NuGet Pack MultiArchitecture
      run: | 
        cd BuildNuGetPackages/BuildNuGetMultiArchitecture/
        dotnet pack --output packages
        cd packages
        ls
  
    - name: NuGet Pack Debian
      run: | 
        cd BuildNuGetPackages/BuildNuGetDebian/
        dotnet pack --output packages
        cd packages
        ls
    