name: NuGet Package CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: NuGet Pack
      run: | 
        cd BuildNuGetPackages/BuildNuGetRaspbian/
        // nuget pack
